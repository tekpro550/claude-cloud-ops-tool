{
  "name": "cloud-ops-tool",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:api": "pnpm --filter @cloud-ops-tool/api start:dev",
    "dev:web": "pnpm --filter @cloud-ops-tool/web dev",
    "dev:portal": "pnpm --filter @cloud-ops-tool/portal dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "preflight": "pnpm install --frozen-lockfile && pnpm --filter @cloud-ops-tool/api exec eslint \"{src,apps,libs,test}/**/*.ts\" && pnpm --filter @cloud-ops-tool/web lint && pnpm --filter @cloud-ops-tool/portal lint"
  },
  "packageManager": "pnpm@10.33.0"
}
